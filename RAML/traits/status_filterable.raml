#%RAML 1.0 Trait

queryParameters:
  status?:
    type: string
    description: |
      `status` is qulified as a unique query parameter for filtering.

      The use of `'&'` (logical AND) and `'|'` (logical OR) may be used to create a compound boolean expression for filtering.
    example: "10.1.2.42"
