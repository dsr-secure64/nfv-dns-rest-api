#%RAML 1.0 Trait

queryParameters:
  ems_address?:
    type: string
    description: |
      `ems_address` is qulified as a unique query parameter for filtering.

      The use of `'&'` (logical AND) and `'|'` (logical OR) may be used to create a compound boolean expression for filtering.
    example: "10.1.2.42"
